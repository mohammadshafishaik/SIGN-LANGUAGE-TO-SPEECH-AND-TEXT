services:
  - type: web
    name: signspeak-ai
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn inference.app_deploy:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.13
